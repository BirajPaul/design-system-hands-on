import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { config, withDesign } from 'storybook-addon-designs';

import ProductItem from '.';

<Meta
	title='Components/ProductItem'
	decorators={[withDesign]}
	parameters={{
		viewport: {
			defaultViewport: 'mobile1',
		},
	}}
/>

## ProductItem

TODO: add component description

export const productArgs = {
	id: 'item1',
	item: {
		name: "Nike Air Force 1 '07",
		price: '€95.00',
		category: 'Men’s Shoe',
		description: 'Men’s Shoe, Black',
		image: '/sampleImages/brian-stalter-G02vZvljukE-unsplash.jpg',
	},
};

<Canvas>
	<Story
		name='Default'
		args={productArgs}
		parameters={{
			// Configuration for design plugin, which allows to display linked Figma frame
			design: config({
				type: 'figma',
				// replace this url with your own design file to link:
				url:
					'https://www.figma.com/file/CjhLWL3fzmonBxpCej4kuo/Workshop-starter-template?node-id=113%3A3209',
			}),
		}}>
		{(args) => <ProductItem {...args} />}
	</Story>
</Canvas>
